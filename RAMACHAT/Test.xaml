<UserControl x:Class="RAMACHAT.Test"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="70" d:DesignWidth="480">

    <Grid HorizontalAlignment="Left"  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Ellipse Grid.Column="0"  Margin="0" Height="70" Width="70" >
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding avatar}" AlignmentX="Center" AlignmentY="Center" />
            </Ellipse.Fill>
        </Ellipse>
        <TextBlock Margin="10,0,0,0" Text="{Binding username}" Grid.Column="1" VerticalAlignment="Center" />
    </Grid>
</UserControl>